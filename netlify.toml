# Build Flutter web i publikacja na Netlify (deploy z Gita).
[[plugins]]
  package = "netlify-plugin-flutter"

[plugins.inputs]
  channel = "stable"

[build]
  command = "bash scripts/netlify_env.sh && flutter build web --release"
  publish = "build/web"
